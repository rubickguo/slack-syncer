# SlackSyncGUI Frontend

This directory contains the React + TypeScript frontend for `slack同步器`.

## Commands

```powershell
npm install
npm run dev
npm run build
```

The frontend is embedded into the Wails desktop app during packaging.
